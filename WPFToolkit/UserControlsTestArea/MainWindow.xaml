<Window x:Class="UserControlsTestArea.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserControlsTestArea"
        xmlns:controls="clr-namespace:WPFToolkit.NetCore.Controls;assembly=WPFToolkit.NetCore"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:PeriodControl Grid.Row="0" Margin="5 5 5 5" SelectedDateFormat="Long" />
        <Button Content="BusyContent test" Grid.Row="1" Margin="5 5 5 5" Command="{Binding MakeBusyCommand}" />
        <controls:BusyControl BusyContent="{Binding BusyContent}" IsBusy="{Binding IsBusy}" Grid.Row="0" Grid.RowSpan="3" />
    </Grid>
</Window>
